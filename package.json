{
  "name": "classy-base",
  "version": "1.0.0",
  "description": "Base for classy scripts",
  "main": "index.js",
  "scripts": {
    "build": "node .config/build.js",
    "watch": "esbuild --watch --bundle --minify --platform=browser --target=es2020 --outdir=build ./src/client/*.ts ./src/server/*.ts"
  },
  "author": "classy",
  "license": "ISC",
  "devDependencies": {
    "@citizenfx/client": "^2.0.6947-1",
    "@citizenfx/server": "^2.0.6947-1",
    "@types/node": "^20.8.9",
    "esbuild": "^0.19.5",
    "typescript": "^5.2.2",
    "fivem-js": "^1.5.2"
  }
}
